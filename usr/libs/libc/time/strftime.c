#include <time.h>
#include <string.h>

size_t strftime(char* s, size_t max,
                const char* format,
				const struct tm* tm)
{
	// TODO: implement all of these time functions

	strcpy(s, "unimpl");
	return max;
}